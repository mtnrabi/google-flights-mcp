# Pinned below 4: fastmcp 4.0.0 moved `ToolResult` out of `fastmcp.tools.tool`,
# so an unpinned build resolves to 4.x and dies on import at first request.
fastmcp>=3.0.0,<4
httpx>=0.27.0
# Only used by the Vercel entrypoint (api/index.py), to hand FastMCP's
# lifespan to the host. Without lifespan the MCP session manager never
# starts and every /mcp request 500s.
fastapi>=0.115.0
